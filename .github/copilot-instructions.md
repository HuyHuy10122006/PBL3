# Copilot Instructions

## Project Guidelines
- User corrected Copilot: link should use `UC_AdminDashboard` instead of Teacher. User prefers responses in Vietnamese.
- Rename the admin homepage control to `UC_AdminDashboard` and ensure the admin menu links to that control when clicking Trang chủ.
- Do not log login events; user prefers login actions not be recorded or shown in system logs.